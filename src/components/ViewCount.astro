---
interface Props {
  path: string;
  prefix?: string;
}

const { path, prefix = ' · ' } = Astro.props;
import styles from '../styles/ViewCount.module.css';
---

<span
  class={styles.count}
  data-view-count
  data-view-path={path}
  data-view-prefix={prefix}
  aria-label="pageviews"
>{prefix}-- views</span>

<script>
  import '../scripts/view-count';
</script>

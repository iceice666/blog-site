import { author, socialDisplayName, socialHref, socialIsExternal } from '../../config';
import styles from '../styles/Sidebar.module.css';

<aside className={styles.sidebar} data-sidebar>
  <div className={styles.widget}>
    <div className={`${styles.paneTitle} ${styles.profileTitle}`}>whoami</div>
    <div className={`${styles.body} ${styles.profile}`}>
      <div className={styles.avatar}>
        <img src={author.avatar} alt={author.name} loading="lazy" />
      </div>
      <div>
        <div className={styles.name}>{author.name}</div>
        <div className={styles.tag}>{author.tagline}</div>
      </div>
    </div>
    <div className={`${styles.body} ${styles.profileLinks}`}>
      <div className={styles.links}>
        {author.socials.map((social) => (
          <a href={socialHref(social)} target={socialIsExternal(social) ? '_blank' : undefined} rel={socialIsExternal(social) ? 'noopener noreferrer' : undefined}>
            {socialDisplayName(social)}
          </a>
        ))}
      </div>
    </div>
  </div>

  <div className={styles.widget}>
    <div className={styles.paneTitle}>active repos</div>
    <div className={styles.body}>
      {props.github.repos.map((repo) => (
        <a className={styles.repo} href={repo.url} target="_blank" rel="noopener noreferrer">
          <div className={styles.repoName}>{repo.name}</div>
          {repo.description ? <div className={styles.repoDesc}>{repo.description}</div> : null}
          <div className={styles.repoLang}>
            <i />
            <span>{repo.language ?? 'unknown'}</span>
            <span className={styles.activity}>{repo.pushed}</span>
          </div>
        </a>
      ))}
      <p className={styles.caption}>
        {props.github.live ? 'top public repos by latest push' : 'fallback repos — live GitHub fetch unavailable at build time'}
      </p>
    </div>
  </div>

  <div className={`${styles.widget} ${styles.finalWidget}`}>
    <div className={styles.paneTitle}>stats</div>
    <div className={styles.body}>
      <div className={styles.stat}><dt>entries</dt><dd>{props.stats.entries}</dd></div>
      <div className={styles.stat}><dt>articles</dt><dd>{props.stats.articles}</dd></div>
      <div className={styles.stat}><dt>words</dt><dd>{props.stats.words.toLocaleString()}</dd></div>
      <div className={styles.stat}><dt>langs</dt><dd>{props.stats.langs.join(' / ')}</dd></div>
      <div className={styles.tags}>
        {props.stats.topTags.map((tag) => (
          <a href={`/archive?tag=${encodeURIComponent(tag)}`} data-vim-target>
            {tag}
          </a>
        ))}
      </div>
    </div>
  </div>
</aside>
